{# empty Twig template #}

{% import "form.macro.twig" as forms %}

{% set path_set = addTwigPath( "applications.Forum.generalviews") %}

<h1 class="component-header">{{ item.title }}</h1>

<div class="business-posted-date">
    <i class="fa fa-clock-o"></i> Posted:  {{ item.date_created|date('d M Y H:i',  document.timezone) }} &nbsp;&nbsp;
</div>

<div> 
    <div class="clearfix"></div>
    <hr>
    <div class="forum-categories-listing">
        {% include "category.topic.list.twig" ignore missing with {'category_id': item.id , 'action': 'latest'}  %}
    </div>
    <div class="clearfix"></div>
    <br>
</div>
    
        {{ forms.input('_token' ) }} 
